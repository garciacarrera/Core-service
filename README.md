"# ChatBackend" 
crear los entitys que faltan, User con maestro y alumno 
validarlo en los schemas 
crear la logica para que cada uno de los entitys pueda hacer un crud, (esto se hace mediante estanciar la base de datos con datasourse en cada caso para el crud)
si no se entiende pedir ayuda a lucas "# Core-service" 
